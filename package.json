{
  "name": "checkin-app",
  "version": "1.0.0",
  "description": "Full-stack team check-in system with Docker and LocalStack",
  "scripts": {
    "build:common": "cd common && npm install && npm run build",
    "dev": "docker-compose up -d --build auth-server",
    "stop": "docker-compose down"
  },
  "author": "Saketh Tadimeti",
  "license": "MIT"
}
